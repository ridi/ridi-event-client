export enum Currency {
  KRW = 'KRW',
  USD = 'USD',
}

export enum ServiceType {
  RIDIBOOKS = 'ridibooks',
  RIDISELECT = 'ridiselect',
}

export enum DeviceType {
  PC = 'pc',
  Mobile = 'mobile',
  Paper = 'paper',
}
